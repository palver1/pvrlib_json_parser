## Documentation
```c
int JP_find_str(char text[], char key[]);
```
Mostly for inner purpose, but I decided to not make it 'static' for now, just in case.  

```c
char *JP_get_value(char text[], char string[]);
```
Main function for getting value by key.